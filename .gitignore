node_modules
dist/
dist-ssr/
.DS_Store
*.local

# Generated on CI from data/ + cache.db — never committed.
public/data/
public/images/courses/

# Crawl cache and working memory. Rebuildable by spending quota again.
data/cache.db
data/cache.db-shm
data/cache.db-wal
data/proposed-courses.yaml

.env
.env.*
!.env.example

*.tsbuildinfo
coverage/
dev-dist/

# Sandbox and map preview output. Rebuildable with `pnpm map:sandbox`.
.map-poc/

# Exported tile collection. Rebuildable with `pnpm tiles:build`.
.tiles/

# Statistics dashboard. Rebuildable with `pnpm stats`.
.stats/

# Agent scratch worktrees.
.claude/worktrees/
.idea
CLAUDE.local.md
keys/
# Product Hunt launch pictures. Rebuildable with `pnpm ph:assets`.
.launch/

# Local launch worksheet — the data typed into a submission form, not documentation.
docs/product-hunt.md
